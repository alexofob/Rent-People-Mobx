import React from 'react';

const styles = {
  rectangle: {
    height: 100,
    weight: '100%',
  },
};
const HouseList = () => (
  <section>
    <div style={styles.rectangle}>House List</div>
  </section>
);

export default HouseList;